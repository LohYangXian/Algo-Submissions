// Hypercarp and the Control Panel

/*
n signal modules arranged in a row

each module containes a certain color

color of ith module is ai

two adj modules of same color may confuse

keep only some modules so no adj same color

u can remove some modules, and swap two adj remaining modules 
at most once

think of an o(n) soln
*/

#include <vector>
#include <iostream>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;

    }
}